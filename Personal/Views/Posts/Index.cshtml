@model IEnumerable<Personal.Models.PersonalPost>

@{
    ViewData["Title"] = "Index";
}

    <div id="postlisting">
        <form class="mt-4 bg-dark p-3 form-post" asp-controller="Posts" asp-action="Create" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <textarea name="content" rows="1" class="form-control post-input" placeholder="How was your day ?'" required></textarea>
                <div class="my-2">
                    <input type="file" name="images" multiple id="inputImages"/>
                    <input type="submit" class="btn btn-outline-warning border-0 float-right" value="Post !" />
                </div>
            </div>
        </form>
        <br />

        <table class="table d-none">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Content)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ImageUrls)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.InsertDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.LastUpdated)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.UserId)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody id="postList">
                @Html.Partial("PartialPosts", Model)
            </tbody>
        </table>

        <p id="loadingtext" style="margin-left:auto; margin-right:auto;display:none;"> </p>
        <input id="hidPage" type="hidden" name="page" value="1"/>
    </div>